<div class="container pt-3">
  <%= render 'search_form', url: exhibitions_path %>
   <!-- 個展一覧 -->
   <div class="row">
     <div class="col-12">
       <div class="row">
         <% if @exhibitions.present? %>
           <%= render @exhibitions %>
         <% else %>
           <p><%= "開催中の個展はありません" %></p>
         <% end %>
       </div>
     </div>
   </div>
   
   <%= link_to "トップページに戻る", root_path %>
 </div>
