<div class="container pt-3">
   <div class="row">
     <div class="col-lg-10 offset-lg-1">
     <%= render 'search_form', url: my_exhibition_exhibitions_path %>

   <!-- 個展一覧 -->
   <div class="row">
     <div class="col-12">
       <div class="row">
         <% if @my_exhibitions.present? %>
           <%= render @my_exhibitions %>
         <% else %>
           <p><%= "開催中の個展はありません" %></p>
         <% end %>
       </div>
     </div>
   </div>
   <%= link_to "トップページに戻る", root_path %>
 </div>
